import React from 'react';
import { mount, render, shallow } from 'enzyme';
import Game from '../game.jsx';

describe('Game component', () => {
    // test('call handleNewGameButton() when the New Game button is clicked', () => {
    //     const handleNewGameButton = jest.fn();
    //     const gameWrapper = mount(
    //         <Game />
    //     );

    //     gameWrapper.find('.new-game-button').simulate('click');
    //     expect(handleNewGameButton).toBeCalled();
    // });
});

